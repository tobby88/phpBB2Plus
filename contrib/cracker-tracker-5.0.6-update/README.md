# CrackerTracker Professional G5 5.0.4 to 5.0.6 update

This directory preserves the documentation and checksums from the reconstructed
update archive `cracker_tracker_update_v504_to_v506_reconstructed.zip`.

Archive SHA-256:

`7D33A9F728677FE3FD04826D8447C1EEDA325CC7248624627406583D63A305A5`

The five runtime files from the archive's `root` directory have been applied to
the `phpBB2` source tree. As phpBB2 Plus uses `fisubsilversh`, the supplied
`templates/subSilver/ctracker/acp/acp_logmanager.tpl` update was applied to
`templates/fisubsilversh/ctracker/acp/acp_logmanager.tpl`.

The reconstructed update does not require a database migration. See
`UPDATE_v504_to_v506_DE.txt`, `DIFF_v504_to_v506.patch`, and `SHA256SUMS.txt`
for provenance and verification details.
