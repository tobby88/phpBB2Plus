Options -Indexes -ExecCGI -Includes

# PA FileDB writes application-internal cache payloads here. They must never
# be requested through the web server.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
